---
title: "Nim version 2.2.12 released"
author: The Nim Team
---

The Nim Team is happy to announce version 2.2.12, the sixth patch release for our stable release, Nim 2.2.

It comes four months after the 2.2.10 release and it contains [142 commits](https://github.com/nim-lang/Nim/compare/v2.2.10...v2.2.12), bringing bugfixes and improvements.

If you're still on Nim 1.6, take a look at the [version 2.0 release article](https://nim-lang.org/blog/2023/08/01/nim-v20-released.html) to see all the features you're missing.
If you've been using Nim 2.0, the [version 2.2 release article](https://nim-lang.org/blog/2024/10/02/nim-220-2010.html) shows the improvements available in Nim 2.2.







## Installing Nim 2.2.12


Check out if the package manager of your OS already ships version 2.2.12 or
install it as described [here](https://nim-lang.org/install.html).

If you have installed a previous version of Nim using `choosenim`,
getting Nim 2.2.12 is as easy as:

```bash
$ choosenim update self
$ choosenim update stable
```

**NOTE:** We recommend that you install and use the latest version of `choosenim`, v0.8.16, available in [our choosenim repo](https://github.com/nim-lang/choosenim).

Alternatively, you can download Nim 2.2.12 from
[our nightlies builds](https://github.com/nim-lang/nightlies/releases/tag/2026-09-08-version-2-2-8e8fbf60693418dc95bb0d762fd660231d08a583).






## Bugfixes

- Fixed "distinctBase(distinct L[int, 100]) does not match L"
  ([#25789](https://github.com/nim-lang/Nim/issues/25789))
- Fixed "`&` (strformat): interprets `{n-1}` as `{n(-1)}`"
  ([#25821](https://github.com/nim-lang/Nim/issues/25821))
- Fixed "[C++] ICE expr(sktype): unknown symbol when bindSym constructor proc in macro"
  ([#25814](https://github.com/nim-lang/Nim/issues/25814))
- Fixed "SIGSEGV with `refc` and RVO"
  ([#25294](https://github.com/nim-lang/Nim/issues/25294))
- Fixed "Poor error message on cast effect violation"
  ([#22950](https://github.com/nim-lang/Nim/issues/22950))
- Fixed "`ProveField` warning with nested `case` object"
  ([#22791](https://github.com/nim-lang/Nim/issues/22791))
- Fixed "incompatible function pointer types C error with uint vs csize_t"
  ([#25617](https://github.com/nim-lang/Nim/issues/25617))
- Fixed "regression from 2.2.10 to `devel` with `lent` and type aliasing"
  ([#25796](https://github.com/nim-lang/Nim/issues/25796))
- Fixed "SIGSEGV in ARC/ORC with repeated use of `{.dirty.}` template and `seq` allocations"
  ([#25693](https://github.com/nim-lang/Nim/issues/25693))
- Fixed "--panics:on drops the nimErr_ check after a closure call (goto exceptions), causing OverflowDefect; regression from #25295"
  ([#25851](https://github.com/nim-lang/Nim/issues/25851))
- Fixed "typeof(result) wrongly counted as use-before-init of result on {.requiresInit.} return types (regression from #25151)"
  ([#25857](https://github.com/nim-lang/Nim/issues/25857))
- Fixed "Missing eqcopy for value copy of value-type case object with ref field"
  ([#25595](https://github.com/nim-lang/Nim/issues/25595))
- Fixed "Iteration on elements of `array`, unlike `seq`, always copies the elements"
  ([#25849](https://github.com/nim-lang/Nim/issues/25849))
- Fixed "Regression from refc on 2.2.x to `devel`, and refc to ORC on 2.2.x/`devel`, of redundant stack usage/copies of return value with conditionals/pointers"
  ([#25850](https://github.com/nim-lang/Nim/issues/25850))
- Fixed "Nested object construction can zero same memory multiple times"
  ([#18238](https://github.com/nim-lang/Nim/issues/18238))
Was unsure about: " fixes  #20811; Nested proc with inner being generic cannot access parameters of outer proc (#25837)"
- Fixed "Nested `proc` with inner being generic cannot access parameters of outer `proc`"
  ([#20811](https://github.com/nim-lang/Nim/issues/20811))
- Fixed "Generic inheritance matching gives `type mismatch` when object has members"
  ([#22936](https://github.com/nim-lang/Nim/issues/22936))
- Fixed "`internal error: environment misses: s` with `iterator`/`nimvm`/variable shadowing"
  ([#25725](https://github.com/nim-lang/Nim/issues/25725))
- Fixed "SIGSEGV in ARC/ORC with repeated use of `{.dirty.}` template and `seq` allocations"
  ([#25693](https://github.com/nim-lang/Nim/issues/25693))
- Fixed "Regression from 2.2.10 to `version-2-2`/`devel` with `proc r(e: ref Exception) {.raises: [IOError].} = raise (ref IOError)(e)`"
  ([#25885](https://github.com/nim-lang/Nim/issues/25885))
- Fixed "unhandled exception: closureiters.nim(336, 17) `c[i].kind == nkType`  [AssertionDefect] with closure iterator  and `except IOError, OSError:`"
  ([#25903](https://github.com/nim-lang/Nim/issues/25903))
- Fixed "using quote inside static block causes "Illegal storage access" error"
  ([#18367](https://github.com/nim-lang/Nim/issues/18367))
- Fixed "Regression from 2.2.10 to `version-2-2`/`devel` with `proc r(e: ref Exception) {.raises: [IOError].} = raise (ref IOError)(e)`"
  ([#25885](https://github.com/nim-lang/Nim/issues/25885))
Was unsure about: " fixes  #22122; Unclear error message for raise of a complex expression (#25899)"
- Fixed "Unclear error message for `raise` of a complex expression"
  ([#22122](https://github.com/nim-lang/Nim/issues/22122))
- Fixed "Manual's "Delegating bind statements" section is nonsense"
  ([#19240](https://github.com/nim-lang/Nim/issues/19240))
Was unsure about: " Issue #22842 is not fixed in this branch"
- Fixed "internal error: getTypeDescAux(tyAnything) with auto in proc type"
  ([#22842](https://github.com/nim-lang/Nim/issues/22842))
- Fixed "Regression from 2.2.10 to `version-2-2`/`devel` with `{.pure.}` enum and generic inline iterator"
  ([#25908](https://github.com/nim-lang/Nim/issues/25908))
- Fixed "`type N {.importc: "const void *".} = pointer` creates order-dependent compilation failure"
  ([#25931](https://github.com/nim-lang/Nim/issues/25931))
- Fixed "emscripten ICE: cannot map the empty seq type to a C type"
  ([#25945](https://github.com/nim-lang/Nim/issues/25945))
- Fixed "`mapIt` pointlessly does extra zeroing which, e.g., `newSeqWith` often avoids"
  ([#25956](https://github.com/nim-lang/Nim/issues/25956))
- Fixed "ImplicitRangeConversion does not recognize constants"
  ([#25608](https://github.com/nim-lang/Nim/issues/25608))
- Fixed "C codegen bug: name mangling collision with nested tuples of matching flattened types"
  ([#25883](https://github.com/nim-lang/Nim/issues/25883))
- Fixed "`.forbids.` type mismatch with closure types for inner proc"
  ([#25976](https://github.com/nim-lang/Nim/issues/25976))
- Fixed "Nimsuggest SIGSEGVs on any type (ideType) query whose target symbol has no usable type"
  ([#25997](https://github.com/nim-lang/Nim/issues/25997))
- Fixed "self-append uses overlapping copyMem and aborts on OpenBSD when mm = refc"
  ([#26007](https://github.com/nim-lang/Nim/issues/26007))
- Fixed "Cannot add members to enum-indexed array of seqs at compile time"
  ([#26000](https://github.com/nim-lang/Nim/issues/26000))
- Fixed "`deepCopy` should not be allowed for non-copyable type"
  ([#26019](https://github.com/nim-lang/Nim/issues/26019))
- Fixed "Double destroy with `{.cursor.}`"
  ([#26010](https://github.com/nim-lang/Nim/issues/26010))
- Fixed "compareExchange applies invalid release semantics on failure"
  ([#26027](https://github.com/nim-lang/Nim/issues/26027))
- Fixed "`{.push overflowChecks: off.}` pragma escapes `when nimvm:` block, affecting runtime code"
  ([#26045](https://github.com/nim-lang/Nim/issues/26045))
- Fixed "`fatal error: invalid kind for firstOrd(tyEmpty)` with `proc p(h: static set[bool]) = discard len(h)`"
  ([#25942](https://github.com/nim-lang/Nim/issues/25942))
- Fixed "incorrect `sink` `requires a copy because it's not the last read of 's'`error in both `refc` and ARC/ORC"
  ([#26023](https://github.com/nim-lang/Nim/issues/26023))
- Fixed "`nimpretty  --indent:3` can inject syntax errors "
  ([#20078](https://github.com/nim-lang/Nim/issues/20078))
- Fixed "performance regression from `refc` to ORC or `--mm:refc --exceptions:goto` with impossible defect-checking"
  ([#26036](https://github.com/nim-lang/Nim/issues/26036))
- Fixed "Nested `{.cast(gcsafe).}` blocks: exiting the inner block cancels the outer block's cast for the statements after it"
  ([#26092](https://github.com/nim-lang/Nim/issues/26092))
- Fixed "`ResultUsed` warning behaves inconsistently with manual characterization with`--warning:ResultUsed:on`"
  ([#26062](https://github.com/nim-lang/Nim/issues/26062))
- Fixed "SIGABRT or SYSASSERT using ARC/ORC with type forward-reference, `distinct`, and `distinctBase` with `-d:useMalloc` or `-d:useSysAssert`"
  ([#26025](https://github.com/nim-lang/Nim/issues/26025))
- Fixed "(N)RVO-related stack usage and memory copying regression from Nim 2.2.x to `version-2-4`/`devel`"
  ([#26112](https://github.com/nim-lang/Nim/issues/26112))
- Fixed "distinct ptr sink'ed too early when only subsequent use is `cast`"
  ([#26123](https://github.com/nim-lang/Nim/issues/26123))
- Fixed "`Error: internal error: expr: param not init k_553648136` with nested generic procs"
  ([#26124](https://github.com/nim-lang/Nim/issues/26124))
- Fixed "refc SIGSEGV on `var r = V(g: true, v: A()); discard move r; GC_fullCollect()` on case object"
  ([#25992](https://github.com/nim-lang/Nim/issues/25992))
- Fixed "Possible memory error ("use after free")"
  ([#26143](https://github.com/nim-lang/Nim/issues/26143))
- Fixed "importc types (eg cint) don't alias properly, causing codegen errors"
  ([#11797](https://github.com/nim-lang/Nim/issues/11797))
- Fixed "`del(seq)`  performs self-assignment and `=destroy` for `del(0)` of 1-length seq"
  ([#26134](https://github.com/nim-lang/Nim/issues/26134))
- Fixed "`=destroy` should accept non-parametrized generic"
  ([#26132](https://github.com/nim-lang/Nim/issues/26132))
- Fixed "JS regression: dockhack.js is invalid"
  ([#26152](https://github.com/nim-lang/Nim/issues/26152))
- Fixed "Regression from 2.2.x to `version-2-4`/`devel` with stack usage increase on `try`/`except` expression with `refc`"
  ([#26119](https://github.com/nim-lang/Nim/issues/26119))
- Fixed "std/encodings: convert silently corrupts stateful encodings (ISO-2022-JP) when the output buffer grows"
  ([#26173](https://github.com/nim-lang/Nim/issues/26173))
- Fixed "`Utf16Char` incorrectly declared as signed"
  ([#26175](https://github.com/nim-lang/Nim/issues/26175))


The complete list of changes is available [here](https://github.com/nim-lang/Nim/compare/v2.2.10...v2.2.12).
